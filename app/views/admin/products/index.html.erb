<ol class="breadcrumb">
<li class="breadcrumb-item">
  <a href="#">Dashboard</a>
</li>
<li class="breadcrumb-item active">Products</li>
<%= link_to 'Add new Product', new_admin_product_path, :class => 'btn btn-success left-right' %>
</ol>

<table class="table">
    <thead>
      <tr>
        <th></th>
        <th>Product Number</th>
        <th>Category</th>
        <th>Name</th>
        <th>Price</th>
        <th>Top rated</th>
      </tr>
    </thead>
    <tbody>
      <% @products.each do |pro| %>
      <tr>
        <td style="vertical-align: middle;"><%= image_tag pro.avatar_url, :size => '100x100' %></td>
        <td style="vertical-align: middle;"><%= pro.product_number %></td>
        <td style="vertical-align: middle;"><%= pro.category %></td>
        <td style="vertical-align: middle;"><%= pro.product_name %></td>
        <td style="vertical-align: middle;"><%= pro.price %></td>
        <td style="vertical-align: middle;"><%= pro.top_rated %></td>
        <td style="vertical-align: middle;"><%= link_to 'show', admin_product_path(pro) %></td>
        <td style="vertical-align: middle;"><%= link_to 'Edit', edit_admin_product_path(pro) %></td>
        <td style="vertical-align: middle;"><%= link_to "Delete", admin_product_path(pro), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
      <% end -%>
    </tbody>
  </table>